# Oakwood Maintenance — environment variables (build-spec §12).
# Copy to .env.local and fill in. NEVER commit real values.

# Database (PostgreSQL — Neon or Supabase)
DATABASE_URL=

# Auth.js / NextAuth
AUTH_SECRET=
AUTH_URL=

# App base URL (used in email links)
APP_URL=

# Email (Resend or SMTP provider equivalent)
RESEND_API_KEY=
FROM_EMAIL="Oakwood Property Management <maintenance@oakwoodpm.co.uk>"
TEAM_INBOX_EMAIL=maintenance@oakwoodpm.co.uk

# Object storage (S3-compatible — Cloudflare R2 or AWS S3)
STORAGE_BUCKET=
STORAGE_ENDPOINT=
STORAGE_ACCESS_KEY_ID=
STORAGE_SECRET_ACCESS_KEY=
STORAGE_PUBLIC_BASE_URL=

# Rate limiting (e.g. Upstash Redis — optional but recommended)
RATE_LIMIT_REDIS_URL=
